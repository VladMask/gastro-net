server:
  port: ${PAYMENT_SERVICE_PORT:8086}

spring:
  application:
    name: payment-service
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:payment-service-db/changelog.xml
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

rest:
  client:
    order-service:
      base-url: http://localhost:${ORDER_SERVICE_PORT:8085}
    reservation-service:
      base-url: http://localhost:${RESERVATION_SERVICE_PORT:8084}

payment:
  service:
    prepayment-percentage: 0.10

common:
  outbox:
    producer:
      kafka-topic: payments
    scheduler:
      lock-at-most-for: 2m
      initial-delay: 10000
      fixed-delay: 5000
      enabled: true

exception:
  dto-factory:
    service-id: payment-service

logging:
  pattern:
    correlation: "[${spring.application.name:},%X{traceId:-},%X{spanId:-}]"

management:
  tracing:
    sampling:
      probability: 1